/**
 * Moved to `@repo/mobile-ui/components/ui/otp-input` when the shop app needed
 * the same control. Re-exported here so rider's existing imports keep working;
 * new code should import from the package directly.
 */
export { OtpInput } from "@repo/mobile-ui/components/ui/otp-input";
