import { PlaceholderScreen } from "../../components/placeholder-screen";

export default function ProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      note="Account, addresses and settings are rebuilt in slice S6."
    />
  );
}
