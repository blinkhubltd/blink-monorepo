import { PlaceholderScreen } from "../components/placeholder-screen";

export default function CartScreen() {
  return (
    <PlaceholderScreen
      title="Basket"
      note="The basket screen is slice S4. Adding and removing items already works from the product grid, and the basket survives sign-in."
      showBack
    />
  );
}
